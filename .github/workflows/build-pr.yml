name: Bygg pull request

on: [pull_request]

env:
  TZ: "Europe/Oslo"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Brukes ukenummer i cache_key da pomene endrer seg alt for ofte.
      # Alternative key med cache fra forrige uke som blir lagret etterpå under CACHE_KEY.
      - name: Set cache key
        run: |
          echo "::set-env name=CACHE_KEY::$(expr $(date +%V) - 1 + 1)"
          echo "::set-env name=PREVIOUS_CACHE_KEY::$(expr $(date +%V) - 1)"
      - name: Henter maven avhengigheter som er cachet
        uses: actions/cache@v1.1.2
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ env.CACHE_KEY }}
          restore-keys: |
            ${{ runner.os }}-maven-${{ env.PREVIOUS_CACHE_KEY }}

      # TODO: Caching fungerer ikke med images
#      - name: Henter docker images som er cachet
#        uses: actions/cache@v1.1.2
#        with:
#          path: /var/lib/docker/overlay2
#          key: ${{ runner.os }}-docker-images-${{ env.CACHE_KEY }}
#          restore-keys: |
#            ${{ runner.os }}-docker-images-${{ env.PREVIOUS_CACHE_KEY }}

      - uses: actions/setup-java@v1
        with:
          java-version: 1.11

      - name: Hent og start Oracle image
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_ACCESS_TOKEN }}
        id: oracle
        shell: bash
        run: |
          echo  $GITHUB_TOKEN | docker login --username "x-access-token" --password-stdin https://docker.pkg.github.com
          echo "Henter og starter Oracle image"
          cd oracle
          docker-compose up --quiet-pull > nohup.out 2>&1 &
          sh -c 'tail -n +0 -f nohup.out | { sed "/Disconnected/q" && kill $$; }' || true

      - name: Set build version
        run: |
          echo "::set-env name=BUILD_VERSION::$(date +%Y.%m.%d.%H%M%S)-$(echo $GITHUB_SHA | cut -c1-7)"


      - name: Hent ut versjon fra forrige issue
        shell: bash
        run: |
          echo ${{ 642 }}
          echo ${{ 642 - 1 }}

          echo "::set-env name=PREVIOUS_VERSION::$( \
            curl -s GET https://api.github.com/repos/navikt/fp-sak/issues/${{ 642 - 1 }} \
              -H "Accept: application/vnd.github.everest-preview+json" \
              -H "Content-Type: application/json"  \
              | grep '"title": "' \
              | cut -d '"' -f 4 | cut -d ' ' -f 3)"

          echo "::set-env name=DIFF::$(git log --pretty=format:"%s; author: %an;" ${{ env.TAG }}...${{ env.PREVIOUS_VERSION }})"

      - name: Build
        env:
          GITHUB_USERNAME: x-access-token
          GITHUB_PASSWORD: ${{ secrets.GITHUB_ACCESS_TOKEN }}
        shell: bash
        run: |
          echo "Building $BUILD_VERSION"
          mvn package --quiet --batch-mode --settings ./.m2/settings.xml -Dchangelist= -Dsha1=-${BUILD_VERSION}
